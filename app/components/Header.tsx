import HeaderContent from './HeaderContent';
import HeaderLayout from './HeaderLayout';

export const Header = () => {
  return (
    <HeaderLayout>
      <HeaderContent />
    </HeaderLayout>
  );
};

export default Header;
