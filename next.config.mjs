/** @type {import('next').NextConfig} */
const nextConfig = {
  // !! WARN !!
  // remove when ready for production, this is just for testing the deployment
  // !! WARN !!
  ignoreBuildErrors: true,
};

export default nextConfig;
